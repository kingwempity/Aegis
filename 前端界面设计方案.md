# Aegis 前端界面设计方案

## 设计理念
基于 AWVS 的简约风格，采用简洁、清晰的设计，避免科技风格元素，注重实用性和易用性。

## 色彩方案（简约白风格）
- **背景色**: `#f5f5f5` (页面背景), `#ffffff` (卡片背景)
- **主色调**: `#1677ff` (Ant Design Blue)
- **文字颜色**: `#262626` (标题), `#595959` (正文), `#8c8c8c` (辅助文字)
- **边框颜色**: `#d9d9d9`
- **阴影**: `0 2px 8px rgba(0, 0, 0, 0.06)`
- **状态颜色**:
  - 成功: `#52c41a`
  - 警告: `#faad14`
  - 错误: `#ff4d4f`
  - 信息: `#1677ff`

## 页面结构

### 1. 整体布局
```
┌─────────────────────────────────────────┐
│ Header (顶部栏)                          │
│ - Logo/标题                              │
│ - 全局搜索                               │
│ - 用户信息                               │
├──────────┬──────────────────────────────┤
│          │                              │
│ Sidebar  │  Content Area                │
│ (侧边栏) │  (内容区域)                    │
│          │                              │
│ - 仪表盘 │  - 根据路由显示不同页面        │
│ - 任务   │                              │
│ - 漏洞   │                              │
│ - 报告   │                              │
│          │                              │
└──────────┴──────────────────────────────┘
```

### 2. Dashboard（仪表盘）
**布局结构**:
- 顶部：统计卡片（4个）
  - 总任务数
  - 运行中任务
  - 发现漏洞数
  - 高风险漏洞
- 中部：图表区域
  - 漏洞分布饼图
  - 扫描趋势折线图
- 底部：最近任务列表（简化表格）

**设计要点**:
- 卡片使用白色背景，轻微阴影
- 数字使用大号字体突出显示
- 图表使用简洁配色，避免花哨效果

### 3. TaskList（任务列表）
**布局结构**:
- 顶部操作栏：
  - 新建扫描按钮（主要操作）
  - 刷新按钮
  - 筛选器（状态、时间范围）
- 表格区域：
  - ID
  - 目标 URL（可点击）
  - 扫描策略
  - 状态（标签显示）
  - 创建时间
  - 操作（查看详情、停止、删除）

**设计要点**:
- 表格使用简洁边框
- 状态标签使用不同颜色区分
- 操作按钮使用链接样式，不占用过多空间

### 4. TaskDetail（任务详情）
**布局结构**:
- 顶部：任务基本信息卡片
  - 目标 URL
  - 状态和进度条
  - 开始/结束时间
  - 操作按钮（暂停、停止）
- 中部：实时日志控制台
  - 自动滚动到底部
  - 时间戳 + 日志内容
  - 不同级别使用不同颜色（INFO/WARN/ERROR）
- 底部：已发现漏洞列表（简化表格）

**设计要点**:
- 日志控制台使用等宽字体
- 进度条清晰显示当前阶段
- 实时更新使用 WebSocket

### 5. VulnAudit（漏洞审计）
**布局结构**:
- 左侧：漏洞列表
  - 漏洞名称
  - 严重程度标签
  - 发现时间
- 右侧：漏洞详情（选中后显示）
  - 基本信息（名称、严重程度、描述）
  - HTTP 报文查看器（左右分栏）
    - 左侧：Request
    - 右侧：Response
    - 高亮显示 Payload 触发位置
  - 修复建议（Markdown 渲染）

**设计要点**:
- 报文查看器使用代码高亮
- 修复建议使用清晰的排版
- 左右分栏布局，便于对比

## 组件设计规范

### Sidebar（侧边栏）
- 宽度：200px（展开）/ 80px（收起）
- 背景：深色 `#001529`
- 菜单项：图标 + 文字
- 激活状态：蓝色背景高亮

### Header（顶部栏）
- 高度：64px
- 背景：白色
- 内容：左侧面包屑，右侧用户信息
- 边框：底部 1px 浅灰边框

### 卡片组件
- 背景：白色
- 圆角：4px
- 阴影：轻微阴影
- 内边距：24px

### 表格组件
- 边框：浅灰色细边框
- 斑马纹：浅灰背景交替
- 悬停：浅蓝背景
- 分页：底部居中显示

### 按钮组件
- 主要按钮：蓝色背景，白色文字
- 次要按钮：白色背景，蓝色边框
- 文字按钮：蓝色文字，无背景
- 危险按钮：红色文字/背景

### 标签组件
- 圆角：4px
- 内边距：4px 8px
- 字体：12px
- 根据状态使用不同颜色

## 交互设计

### 加载状态
- 使用 Ant Design 的 Spin 组件
- 显示加载文字提示

### 空状态
- 显示友好的空状态提示
- 提供操作建议

### 错误处理
- 使用 message 组件显示错误提示
- 错误信息清晰明确

### 响应式设计
- 最小宽度：1200px
- 侧边栏在小屏幕可收起
- 表格支持横向滚动

## 动画效果
- 页面切换：淡入淡出（200ms）
- 按钮点击：轻微缩放反馈
- 表格行悬停：背景色渐变（150ms）
- 避免过度动画，保持简洁

## 字体规范
- 标题：16px/18px，加粗
- 正文：14px，常规
- 辅助文字：12px，常规
- 代码：13px，等宽字体

## 间距规范
- 页面边距：24px
- 卡片间距：16px
- 组件内边距：16px/24px
- 表单元素间距：16px
