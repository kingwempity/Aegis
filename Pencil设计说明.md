# Aegis 前端界面 Pencil 设计说明

## 设计目标
基于 AWVS 的简约风格，设计 Aegis 漏洞检测系统的前端界面，避免科技风格元素，注重实用性和易用性。

## 需要设计的页面

### 1. Layout（整体布局）
- **Sidebar（侧边栏）**：
  - 宽度：200px（展开）/ 80px（收起）
  - 背景色：#001529（深蓝灰）
  - Logo区域：顶部，高度64px
  - 菜单项：
    - 仪表盘（Dashboard）
    - 任务管理（Tasks）
    - 漏洞审计（Vulnerabilities）
    - 报告中心（Reports）
    - 设置（Settings）
  - 激活状态：蓝色背景高亮

- **Header（顶部栏）**：
  - 高度：64px
  - 背景：白色 #ffffff
  - 左侧：面包屑导航
  - 右侧：全局搜索框 + 用户头像下拉菜单
  - 底部边框：1px #e8e8e8

### 2. Dashboard（仪表盘）
**布局**：
- 顶部统计卡片（4个，横向排列）：
  1. 总任务数：数字 + 图标
  2. 运行中任务：数字 + 进度指示
  3. 发现漏洞数：数字 + 趋势箭头
  4. 高风险漏洞：数字 + 警告图标

- 中部图表区域（2列布局）：
  - 左侧：漏洞分布饼图（按严重程度）
  - 右侧：扫描趋势折线图（最近7天）

- 底部：最近任务列表（简化表格，显示最近5条）

**设计要点**：
- 卡片使用白色背景，圆角4px，轻微阴影
- 数字使用24px大字体，加粗
- 图表使用简洁配色，避免渐变和3D效果

### 3. TaskList（任务列表页）
**布局**：
- 顶部操作栏：
  - 左侧：标题"任务管理"
  - 右侧：新建扫描按钮（主要按钮）+ 刷新按钮

- 筛选栏：
  - 状态筛选（全部/运行中/已完成/失败）
  - 时间范围选择器
  - 搜索框（按URL搜索）

- 表格区域：
  - 列：ID、目标URL、扫描策略、状态、创建时间、操作
  - 状态标签：不同颜色（运行中-蓝色、已完成-绿色、失败-红色）
  - 操作列：查看详情、停止、删除（图标按钮）

**设计要点**：
- 表格使用简洁边框，斑马纹背景
- 状态标签使用圆角标签样式
- 操作按钮使用图标，悬停显示文字提示

### 4. TaskDetail（任务详情页）
**布局**：
- 顶部信息卡片：
  - 目标URL（大号显示）
  - 状态标签 + 进度条
  - 开始时间/结束时间
  - 操作按钮：暂停、停止、删除

- 中部标签页：
  - 实时日志（默认选中）
  - 已发现漏洞
  - 扫描配置

- 日志控制台：
  - 背景：深色 #1e1e1e
  - 文字：等宽字体，14px
  - 自动滚动到底部
  - 不同级别颜色：INFO-白色、WARN-黄色、ERROR-红色
  - 时间戳：灰色，12px

- 漏洞列表：
  - 简化表格：漏洞名称、严重程度、发现时间、操作

**设计要点**：
- 进度条清晰显示当前阶段
- 日志控制台使用等宽字体，便于阅读
- 实时更新使用 WebSocket 连接

### 5. VulnAudit（漏洞审计页）
**布局**：
- 左侧：漏洞列表（宽度300px）
  - 搜索框
  - 筛选器（按严重程度）
  - 漏洞列表：
    - 漏洞名称（可点击）
    - 严重程度标签
    - 发现时间

- 右侧：漏洞详情（选中后显示）
  - 顶部：漏洞名称 + 严重程度标签
  - 基本信息卡片：
    - 描述
    - 影响URL
    - 发现时间
  - HTTP报文查看器（左右分栏）：
    - 左侧：Request（请求）
    - 右侧：Response（响应）
    - 高亮显示Payload触发位置
    - 使用代码高亮显示
  - 修复建议卡片：
    - Markdown渲染
    - 清晰的排版和代码块

**设计要点**：
- 报文查看器使用代码高亮，便于阅读
- Payload高亮使用黄色背景
- 修复建议使用清晰的层次结构

## 色彩规范
- 背景：页面 #f5f5f5，卡片 #ffffff
- 主色：#1677ff（Ant Design Blue）
- 文字：标题 #262626，正文 #595959，辅助 #8c8c8c
- 边框：#d9d9d9
- 状态颜色：
  - 成功：#52c41a
  - 警告：#faad14
  - 错误：#ff4d4f
  - 信息：#1677ff

## 字体规范
- 标题：16px/18px，加粗
- 正文：14px，常规
- 辅助文字：12px，常规
- 代码：13px，等宽字体（Monaco, Consolas）

## 间距规范
- 页面边距：24px
- 卡片间距：16px
- 组件内边距：16px/24px
- 表单元素间距：16px

## 交互规范
- 按钮悬停：轻微背景色变化
- 表格行悬停：浅蓝背景 #e6f7ff
- 页面切换：淡入淡出 200ms
- 避免过度动画，保持简洁
