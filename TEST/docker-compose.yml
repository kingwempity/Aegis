services:
  sqli-test:
    build: .
    container_name: aegis-sqli-test
    ports:
      - "8083:8080"
    volumes:
      - ./test_data:/app/test_data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
