下面用通用概念说明「模拟攻击」在动态漏洞检测里怎么运转，并对应到本仓库 ScannerEngine 的实现，便于你对照代码。

1. 概念：什么是「模拟攻击」+「动态检测」
动态检测：对正在运行的 Web 应用发真实 HTTP 请求，根据实时响应判断是否可能存在漏洞（与静态分析源码相对）。
模拟攻击：在授权、合规前提下，构造探测性 / 验证型输入（payload），观察是否出现可预期的异常表现（错误信息、状态码、响应体特征等），不追求真实利用与破坏。
二者合起来：用无害或低风险的请求去「试探」线上行为，再用规则从响应里推断漏洞信号。

2. 运行逻辑（与本项目一致的主链路）
整体在 scanner/engine/core.py 里写得很直白：加载插件 → 初始探测建上下文 → 按插件并发扫描 → 每条请求里「路径排序 × Payload 变体」→ 发请求 → 匹配器判定 → 学习/发现路径 → 可选二次发现扫描。

核心扫描逻辑：遍历插件 -> 生成攻击脚本 -> 路径优先调度 -> 发送请求 -> 匹配漏洞。
集成优化功能：
1. 智能Payload生成与编码
2. 多维度路径优先级探索
3. 上下文感知攻击策略
4. 动态路径发现与学习


阶段 A：准备——插件与引擎
1、从目录加载所有合法 YAML 插件（每条插件含 requests、路径、matchers 等）。
2、构造 AttackScriptGenerator（Payload 变体/编码）、AttackPathExplorer（路径打分与排序）。

阶段 B：初始探测——「动态」上下文
1、对目标 GET 一次首页（或入口 URL）。
2、用 ContextAwareEngine.build_context 从响应头/体推断技术栈、表单与 CSRF 等，写入 AttackContext。
3、把上下文交给 Payload 生成器；若开启发现，从 HTML 里 discover_paths 抽链接/路径，供后续扩展扫描。


阶段 C：模拟攻击主循环——插件 × 请求模板
对每个插件的每个 request：

1、前置条件（_check_preconditions）：例如要求已识别到某技术栈才跑对应插件，避免无效请求。
2、build_payloads：按插件里的基础 payload + 策略（如 aggressive 时变异）+ 编码，得到有序 PayloadVariant 列表。
3、path_explorer.rank：对插件里多条 path 做多维度打分排序，优先试「更敏感、更新、历史更有效」的路径。
4、双重循环：对每个 PathCandidate × 每个 PayloadVariant 调用 _try_attack（即一次「模拟攻击」尝试）。


阶段 D：单次尝试——发请求与判定
在 _try_attack 中：
1、把 payload 填入 URL/body 模板（render_path / render_body）。
2、若有 CSRF token，自动加到请求头。
3、_request_in_scope：只向目标域内发请求（范围控制，避免误扫第三方）。
4、_check_matchers：用插件声明的 word / status / regex / size / binary 等对响应做与/或组合判断；命中则记为疑似漏洞。
5、record_result：把该路径是否「命中」记入历史，供后续 calculate_score 学习权重。
6、若开启发现，从本次响应再 discover_paths，积累待扫 URL。


阶段 E：扫描结束后的「动态扩展」（可选）
若 enable_discovery 且统计上有新路径，再跑 _discovery_scan：对高优先级发现路径发 GET，并用正则做敏感信息泄露等轻量检测——仍属于「对运行中站点的动态探测」，但不一定再走完整插件 payload。


3. 逻辑流程图
flowchart TD
  A[加载 YAML 插件] --> B[初始 GET 目标]
  B --> C[ContextAwareEngine 构建上下文]
  C --> D[可选: 从响应发现新路径]
  D --> E[遍历插件 requests]
  E --> F{前置条件?}
  F -->|否| E
  F -->|是| G[生成 Payload 变体]
  G --> H[路径 rank 排序]
  H --> I[对每个 path × payload 发 HTTP]
  I --> J{matchers 命中?}
  J -->|是| K[记录 ScanResult 漏洞]
  J -->|否| L[记录学习 + 可选再发现路径]
  K --> L
  L --> E
  E --> M[可选 discovery_scan]


4. 小结（一句话逻辑）
动态漏洞检测依赖真实交互；模拟攻击负责批量、可编排地构造这些交互（插件定义「测什么」、Payload 与路径调度决定「先怎么测」、matchers 决定「响应算不算中招」）。本系统中，这一闭环集中在 ScannerEngine.run() → _initial_probe → _execute_plugins → _scan_with_plugin → _try_attack → _check_matchers，并在同一流程里叠加上下文感知、路径优先级与学习、动态路径发现。